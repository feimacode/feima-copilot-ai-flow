# Assets Directory

## Icon Required

The extension needs a 128x128px PNG icon at `icon.png`.

For now, you can:
1. Create a simple icon using an online tool
2. Or temporarily remove the `"icon"` field from package.json until ready

Recommended: Use a VS Code/Copilot themed icon with a panel/discussion visual element.
